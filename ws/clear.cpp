#include <bits/stdc++.h>
#include <time.h>
#include <windows.h>

using namespace std;
FILE *r, *w;

int main() {
    int i = 0;
    while (1) {
        Sleep(1000);
        w = fopen("in.txt", "w");
        fclose(w);
    }

    return 0;
}